#include<iostream>

double radice(const double& value, const unsigned int& n);

int main()
{
    std::cout <<"Ciao, mondo!"<< std::endl;
    std::cout << "radice(3.2,2) = " << radice(3.2, 2)<<std::endl; 
    return 0;

}